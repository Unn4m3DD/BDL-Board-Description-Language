const sleep = (milliseconds) => {
  return new Promise(resolve => setTimeout(resolve, milliseconds))
}
export default async (context, table) => {
  let v0 = new ABDLVar(2);
  let v1 = new ABDLVar(1);
  let v3 = new ABDLVar(10);
  let v2 = v1.less_than(v3);
  while (v2.not_equal(0)) {
      let v4 = new ABDLVar(3);
      let v5 = v4.add(v1);
      v1 = v5;
      v2 = v1.less_than(v3);
  }
  let v6 = new ABDLVar(10);
  v0 = v6;
}